name: CI

on:
  push:
    branches: [main]
  pull_request:

# Cancel superseded runs on the same ref.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend (uv · Python 3.14)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v7
      - name: Install uv
        uses: astral-sh/setup-uv@v8.2.0
        with:
          enable-cache: true
      - name: Sync dependencies (uv installs Python 3.14 per pyproject)
        run: uv sync --frozen
      - name: Lint (ruff)
        run: uv run ruff check .
      - name: Format check (ruff)
        run: uv run ruff format --check .
      - name: Run unit tests
        run: uv run pytest tests -v

  frontend:
    name: Frontend (pnpm · Node 22)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v7
      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Lint + format check (biome)
        run: pnpm lint
      - name: Type-check
        run: pnpm typecheck
      - name: Test (vitest)
        run: pnpm test
      - name: Build
        run: pnpm build

  # docker:
  #   name: Docker images build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v7
  #     - name: Set up Buildx
  #       uses: docker/setup-buildx-action@v4
  #     # Build both images directly (no compose -> no .env interpolation needed).
  #     # Backend context is the repo root so executables/ is reachable.
  #     - name: Build backend image
  #       uses: docker/build-push-action@v7
  #       with:
  #         context: .
  #         file: backend/Dockerfile
  #         push: false
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max
  #     - name: Build frontend image
  #       uses: docker/build-push-action@v7
  #       with:
  #         context: ./frontend
  #         file: frontend/Dockerfile
  #         push: false
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max
